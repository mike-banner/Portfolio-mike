---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';

import ecommerceImg from '../assets/images/project-ecommerce.png';
import vtcImg from '../assets/images/project-vtc.png';
import briquerougeImg from '../assets/images/project-briquerouge.png';
import headshotImg from '../assets/images/headshot.png';
---

<Layout
  title="Fullstack Engineer"
  description="Modern Fullstack Engineer. Building production-grade multi-tenant SaaS with TypeScript, React, Astro, Terraform, Supabase, Cloudflare."
>

  <header class="header" role="banner">
    <nav class="nav" aria-label="Main Navigation">
      <a href="#" class="logo" aria-label="Back to top">M<span class="dot">.</span></a>

      <ul class="nav-links" role="list">
        <li><a href="#work">Work</a></li>
        <li><a href="#skills">Skills</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>

      <button class="hamburger" id="menuBtn" aria-label="Open Menu" aria-expanded="false" aria-controls="mobileDrawer">
        <span></span><span></span><span></span>
      </button>
    </nav>
  </header>

  <div class="drawer" id="mobileDrawer" role="dialog" aria-modal="true" aria-label="Navigation Menu" hidden>
    <div class="drawer-overlay" id="drawerOverlay"></div>
    <nav class="drawer-nav">
      <button class="drawer-close" id="drawerClose" aria-label="Close Menu">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
      <ul role="list">
        <li><a href="#work" class="drawer-link">Work</a></li>
        <li><a href="#skills" class="drawer-link">Skills</a></li>
        <li><a href="#contact" class="drawer-link">Contact</a></li>
      </ul>
    </nav>
  </div>

  <section class="hero" aria-labelledby="hero-heading">
    <div class="hero-inner">
      <div class="hero-image-wrapper">
        <Image
          src={headshotImg}
          alt="Michael Banicles"
          width={400}
          height={400}
          class="hero-headshot"
          fetchpriority="high"
        />
        <div class="hero-glow"></div>
      </div>

      <div class="hero-content">
        <div class="badge" role="status">
          <span class="badge-dot" aria-hidden="true"></span>
          Available for new opportunities
        </div>

        <h1 id="hero-heading">
          <span class="hero-name">Michael Banicles</span>
          <span class="hero-title">Fullstack Engineer<br><span class="hero-accent">SaaS & Cloud Architecture</span></span>
        </h1>

        <p class="hero-desc">
          Building production-grade multi-tenant SaaS. Focused on scalable architectures, immutable data layers, and clean component systems using TypeScript, React, Astro, Terraform, and Supabase.
        </p>

        <div class="hero-tags" aria-label="Core Stack">
          <span class="tag">TypeScript</span>
          <span class="tag">React & Astro</span>
          <span class="tag">Vue & Nuxt</span>
          <span class="tag">Supabase</span>
          <span class="tag">Terraform</span>
          <span class="tag">Cloudflare</span>
        </div>

        <div class="hero-actions">
          <a href="#work" class="btn btn-primary">Selected Work</a>
          <a href="https://github.com/mike-banner" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
            GitHub
          </a>
        </div>
      </div>
    </div>
  </section>

  <section id="work" class="section" aria-labelledby="projects-heading">
    <div class="section-header">
      <h2 id="projects-heading" class="section-title">Selected Work</h2>
    </div>

    <div class="project-filters" role="group" aria-label="Filter projects">
      <button class="filter-btn active" data-filter="saas">SaaS & Apps</button>
      <button class="filter-btn" data-filter="wordpress">CMS</button>
      <button class="filter-btn" data-filter="all">All</button>
    </div>

    <div class="bento-grid">
      <article class="project-card card-large" data-tech="saas all">
        <div class="card-body card-body-split">
          <div class="card-content-side">
            <h3>VTC HUB — SaaS Monorepo</h3>
            <p>B2B Multi-tenant SaaS platform featuring a dynamic frontend engine capable of generating thousands of white-label driver websites. Fully managed via a central backoffice, it includes realtime route pricing, immutable accounting ledgers, and strict Row Level Security.</p>
            
            <ul class="tech-pills" aria-label="Technologies used">
              <li>Astro 5</li><li>React 19</li><li>Supabase</li><li>Cloudflare Pages</li><li>Tailwind v4</li>
            </ul>

            <div class="card-actions" style="margin-top: 1rem;">
              <a href="/projects/vtc-hub" class="btn btn-primary" style="padding: 0.6rem 1.25rem; font-size: 0.85rem; width: fit-content;">Read Case Study &rarr;</a>
            </div>
          </div>
          <div class="card-image-side">
             <div class="code-preview">
                <div class="code-header">
                  <span class="dot-red"></span><span class="dot-yellow"></span><span class="dot-green"></span>
                </div>
                <div class="code-body" style="font-size: 0.8rem;">
                  <span style="color: var(--text-muted);">// Multi-tenant data isolation</span>
                  <br><span class="kw">CREATE POLICY</span> <span class="str">"Tenant Isolation"</span>
                  <br>&nbsp;&nbsp;<span class="kw">ON</span> <span class="fn">public.trips</span>
                  <br>&nbsp;&nbsp;<span class="kw">FOR ALL</span>
                  <br>&nbsp;&nbsp;<span class="kw">USING</span> (
                  <br>&nbsp;&nbsp;&nbsp;&nbsp;tenant_id = <span class="fn">auth.jwt</span>()->><span class="str">'app_metadata'</span>->><span class="str">'tenant'</span>
                  <br>&nbsp;&nbsp;);
                </div>
             </div>
          </div>
        </div>
      </article>

      <article class="project-card card-large" data-tech="saas all">
        <div class="card-body card-body-split">
          <div class="card-content-side">
            <h3>Bâti-Axe — Construction Marketplace</h3>
            <p>A double-sided marketplace connecting homeowners with verified construction professionals. Features include a lead-generation pipeline, magic-link authentication, Stripe webhooks for professional subscriptions, and Infrastructure as Code deployed via Terraform.</p>
            <ul class="tech-pills" aria-label="Technologies used">
              <li>Nuxt 4</li><li>Vue 3</li><li>Terraform</li><li>Supabase</li><li>Stripe</li><li>R2 Storage</li>
            </ul>
            <div class="card-actions" style="margin-top: 1rem;">
              <a href="/projects/bati-axe" class="btn btn-primary" style="padding: 0.6rem 1.25rem; font-size: 0.85rem; width: fit-content;">Read Case Study &rarr;</a>
            </div>
          </div>
          <div class="card-image-side">
             <div class="code-preview">
                <div class="code-header">
                  <span class="dot-red"></span><span class="dot-yellow"></span><span class="dot-green"></span>
                </div>
                <div class="code-body" style="font-size: 0.8rem;">
                  <span style="color: var(--text-muted);">// Edge Function: Stripe Webhook</span>
                  <br><span class="kw">if</span> (event.type === <span class="str">'invoice.paid'</span>) {'{'}
                  <br>&nbsp;&nbsp;<span class="kw">await</span> <span class="fn">supabase</span>
                  <br>&nbsp;&nbsp;&nbsp;&nbsp;.from(<span class="str">'professional_leads'</span>)
                  <br>&nbsp;&nbsp;&nbsp;&nbsp;.update({'{'} is_unlocked: <span class="kw">true</span> {'}'})
                  <br>&nbsp;&nbsp;&nbsp;&nbsp;.eq(<span class="str">'customer_id'</span>, event.data.customer);
                  <br>{'}'}
                </div>
             </div>
          </div>
        </div>
      </article>

      <article class="project-card card-small" data-tech="wordpress all">
        <div class="card-image card-image--short">
          <Image
            src={ecommerceImg}
            alt="Meliponini E-Commerce"
            width={600}
            height={340}
            format="avif"
            class="proj-img"
          />
        </div>
        <div class="card-body">
          <h3>Meliponini E-Commerce</h3>
          <p>Headless boutique synced via GraphQL with WooCommerce.</p>
          <ul class="tech-pills" aria-label="Technologies used">
            <li>Astro</li><li>GraphQL</li><li>WooCommerce</li>
          </ul>
        </div>
      </article>

      <article class="project-card card-small" data-tech="wordpress all">
        <div class="card-image card-image--short">
          <Image
            src={briquerougeImg}
            alt="Studio La Brique Rouge"
            width={600}
            height={340}
            format="avif"
            class="proj-img"
          />
        </div>
        <div class="card-body">
          <h3>Studio La Brique Rouge</h3>
          <p>Highly optimized portfolio and booking tunnel for a media studio.</p>
          <ul class="tech-pills" aria-label="Technologies used">
            <li>WordPress</li><li>WebPerf</li>
          </ul>
        </div>
      </article>
    </div>
  </section>

  <section id="skills" class="section section--alt" aria-labelledby="skills-heading">
    <div class="section-header">
      <h2 id="skills-heading" class="section-title">Technical Expertise</h2>
    </div>

    <div class="skills-grid">
      <div class="skill-card">
        <div class="skill-icon" aria-hidden="true">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
        </div>
        <h3>Cloud & Infrastructure</h3>
        <ul>
          <li>Terraform (IaC)</li>
          <li>Cloudflare (Pages, Workers, R2)</li>
          <li>Docker & VPS Deployment (Coolify)</li>
          <li>GitHub Actions & CI/CD Pipelines</li>
        </ul>
      </div>

      <div class="skill-card">
        <div class="skill-icon" aria-hidden="true">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z"/></svg>
        </div>
        <h3>Backend & Data</h3>
        <ul>
          <li>Supabase (PostgreSQL, RLS, Edge Functions)</li>
          <li>Node.js & Nitro Server</li>
          <li>Stripe API & Webhooks</li>
          <li>GraphQL & REST API Design</li>
        </ul>
      </div>

      <div class="skill-card">
        <div class="skill-icon" aria-hidden="true">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
        </div>
        <h3>Frontend Engineering</h3>
        <ul>
          <li>React 19 & Astro 5</li>
          <li>Vue 3 & Nuxt 4</li>
          <li>TypeScript & modern JavaScript</li>
          <li>Tailwind CSS & shadcn/ui</li>
          <li>Web Performance & Core Web Vitals</li>
        </ul>
      </div>
    </div>
  </section>

  <section id="contact" class="contact-section" aria-labelledby="contact-heading">
    <div class="contact-inner">
      <div class="section-header">
        <h2 id="contact-heading" class="section-title">Get in touch</h2>
        <p class="contact-subtitle">Freelance projects — rate on request.</p>
      </div>

    <div class="contact-wrap">
      <form class="contact-form" id="contactForm" novalidate>
        <div class="form-row">
          <div class="form-group">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required placeholder="Your name" autocomplete="name" />
          </div>
          <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required placeholder="your@email.com" autocomplete="email" />
          </div>
        </div>
        <div class="form-group">
          <label for="message">Message</label>
          <textarea id="message" name="message" rows="5" required placeholder="How can I help you?"></textarea>
        </div>

        <div id="formStatus" class="form-status" role="alert" aria-live="polite" hidden></div>

        <button type="submit" class="btn btn-primary btn-submit" id="submitBtn">
          Send Message
        </button>
      </form>
    </div>
    </div>
  </section>

  <footer class="footer" role="contentinfo">
    <p>&copy; {new Date().getFullYear()} Michael Banicles.</p>
    <p class="footer-sub">Built with Astro. Hosted on Cloudflare Pages.</p>
  </footer>

</Layout>

<script>
  const filterButtons = document.querySelectorAll('.filter-btn');
  const projectCards  = document.querySelectorAll('.project-card');

  filterButtons.forEach(button => {
    button.addEventListener('click', () => {
      filterButtons.forEach(btn => btn.classList.remove('active'));
      button.classList.add('active');

      const filter = button.getAttribute('data-filter') || 'saas';

      projectCards.forEach(card => {
        const techAttr = card.getAttribute('data-tech') || '';
        const techs = techAttr.split(' ');
        
        if (filter === 'all' || techs.includes(filter)) {
          card.classList.remove('filtered-out');
        } else {
          card.classList.add('filtered-out');
        }
      });
    });
  });

  const defaultActive = document.querySelector('.filter-btn.active') as HTMLButtonElement | null;
  if (defaultActive) {
    defaultActive.click();
  }

  const menuBtn = document.getElementById('menuBtn') as HTMLButtonElement | null;
  const drawer  = document.getElementById('mobileDrawer') as HTMLElement | null;
  const overlay = document.getElementById('drawerOverlay');
  const closeBtn = document.getElementById('drawerClose');
  const drawerLinks = document.querySelectorAll('.drawer-link');

  function openDrawer() {
    if (!drawer || !menuBtn) return;
    drawer.hidden = false;
    menuBtn.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }
  function closeDrawer() {
    if (!drawer || !menuBtn) return;
    drawer.hidden = true;
    menuBtn.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }

  menuBtn?.addEventListener('click', openDrawer);
  closeBtn?.addEventListener('click', closeDrawer);
  overlay?.addEventListener('click', closeDrawer);
  drawerLinks.forEach(l => l.addEventListener('click', closeDrawer));
  document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeDrawer(); });

  const form      = document.getElementById('contactForm') as HTMLFormElement | null;
  const statusDiv = document.getElementById('formStatus') as HTMLElement | null;
  const submitBtn = document.getElementById('submitBtn') as HTMLButtonElement | null;

  if (form && statusDiv && submitBtn) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      submitBtn.disabled = true;
      submitBtn.textContent = 'Sending...';

      if (statusDiv) {
        statusDiv.hidden = false;
        statusDiv.className = 'form-status info';
        statusDiv.textContent = 'Sending your message...';
      }

      const data = Object.fromEntries(new FormData(form).entries());

      try {
        const response = await fetch('/api/contact', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
        });

        if (response.ok || response.status === 404) {
          if (statusDiv) {
            statusDiv.className = 'form-status success';
            statusDiv.textContent = 'Message sent! I will get back to you shortly.';
          }
          form.reset();
        } else {
          throw new Error('Transmission error');
        }
      } catch {
        if (statusDiv) {
          statusDiv.className = 'form-status error';
          statusDiv.textContent = "An error occurred. Please try again or reach out directly.";
        }
      } finally {
        submitBtn.disabled = false;
        submitBtn.textContent = 'Send Message';
      }
    });
  }
</script>

<style>
  .section {
    padding: 6rem 1.25rem;
    width: 100%;
    border-bottom: 1px solid var(--border);
  }
  .section--alt {
    background-color: var(--bg-surface);
  }

  .header {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: var(--z-nav);
    background: rgba(3, 7, 18, 0.8);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border);
  }

  .nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 1.25rem;
  }

  .logo {
    font-family: var(--font-heading);
    font-size: 1.6rem;
    font-weight: 800;
    color: var(--text-primary);
    text-decoration: none;
    letter-spacing: -0.03em;
  }
  .logo .dot { color: var(--accent); }

  .nav-links {
    display: none;
    list-style: none;
    gap: 2.5rem;
  }
  .nav-links a {
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    transition: color 0.2s;
  }
  .nav-links a:hover { color: var(--text-primary); }

  .hamburger {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 40px;
    height: 40px;
    background: none;
    border: 1px solid var(--border);
    border-radius: 8px;
    cursor: pointer;
    padding: 0 10px;
    transition: border-color 0.2s;
  }

  @media (min-width: 768px) {
    .nav-links { display: flex; }
    .hamburger { display: none; }
  }
  .hamburger:hover { border-color: var(--border-hover); }
  .hamburger span {
    display: block;
    height: 2px;
    background: var(--text-primary);
    border-radius: 2px;
  }

  .drawer {
    position: fixed;
    inset: 0;
    z-index: var(--z-drawer);
  }
  .drawer-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(4px);
  }
  .drawer-nav {
    position: absolute;
    top: 0; right: 0;
    width: min(320px, 85vw);
    height: 100%;
    background: var(--bg-elevated);
    border-left: 1px solid var(--border);
    padding: 2rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
  .drawer-close {
    align-self: flex-end;
    background: none;
    border: 1px solid var(--border);
    border-radius: 8px;
    color: var(--text-secondary);
    cursor: pointer;
    padding: 0.4rem;
    transition: color 0.2s, border-color 0.2s;
  }
  .drawer-close:hover { color: var(--text-primary); border-color: var(--border-hover); }
  .drawer-nav ul { list-style: none; display: flex; flex-direction: column; gap: 0.25rem; }
  .drawer-link {
    display: block;
    padding: 0.85rem 1rem;
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 1.05rem;
    font-weight: 500;
    border-radius: 10px;
    transition: background 0.2s, color 0.2s;
  }
  .drawer-link:hover {
    background: rgba(255, 255, 255, 0.05);
    color: var(--text-primary);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 1.75rem;
    border-radius: 10px;
    font-family: var(--font-body);
    font-size: 0.95rem;
    font-weight: 600;
    text-decoration: none;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s, background 0.2s, border-color 0.2s;
    border: 1px solid transparent;
  }
  .btn:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
  }
  .btn-primary {
    background: var(--accent);
    color: var(--bg-base);
    box-shadow: 0 4px 16px var(--accent-glow);
  }
  .btn-primary:hover:not(:disabled) {
    background: var(--accent-light);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px var(--accent-glow);
  }
  .btn-primary:disabled { opacity: 0.6; cursor: not-allowed; }
  .btn-secondary {
    background: transparent;
    color: var(--text-primary);
    border-color: var(--border);
  }
  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.2);
    transform: translateY(-2px);
  }

  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 8rem 1.25rem 4rem;
    background:
      radial-gradient(ellipse 60% 50% at 80% 20%, rgba(255, 255, 255, 0.05), transparent),
      radial-gradient(ellipse 50% 40% at 10% 80%, rgba(255, 255, 255, 0.03), transparent);
  }
  
  .hero-inner {
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
    align-items: center;
  }

  @media (min-width: 860px) {
    .hero-inner {
      grid-template-columns: 320px 1fr;
      gap: 5rem;
    }
  }

  .hero-image-wrapper {
    position: relative;
    width: 280px;
    height: 280px;
    margin: 0 auto;
    animation: fadeUp 0.7s cubic-bezier(0.16, 1, 0.3, 1) both;
  }
  
  @media (min-width: 860px) {
    .hero-image-wrapper {
      width: 100%;
      height: auto;
      aspect-ratio: 1;
      margin: 0;
    }
  }

  .hero-headshot {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 50%;
    border: 2px solid var(--border);
    position: relative;
    z-index: 2;
    background-color: var(--bg-surface);
  }

  .hero-glow {
    position: absolute;
    inset: -20px;
    background: radial-gradient(circle, var(--accent-glow) 0%, transparent 70%);
    z-index: 1;
    border-radius: 50%;
    opacity: 0.6;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(34, 197, 94, 0.08);
    color: #4ade80;
    border: 1px solid rgba(34, 197, 94, 0.2);
    padding: 0.4rem 1rem;
    border-radius: 9999px;
    font-size: 0.78rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    margin-bottom: 1.75rem;
    width: fit-content;
  }
  .badge-dot {
    width: 7px; height: 7px;
    background: #4ade80;
    border-radius: 50%;
    animation: pulse-dot 2s ease-in-out infinite;
  }
  @keyframes pulse-dot {
    0%, 100% { opacity: 1; transform: scale(1); }
    50%       { opacity: 0.5; transform: scale(0.75); }
  }

  .hero-name {
    display: block;
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 800;
    color: var(--text-primary);
    line-height: 1.1;
    margin-bottom: 0.5rem;
  }
  .hero-title {
    display: block;
    font-size: clamp(1.2rem, 3vw, 1.8rem);
    font-weight: 500;
    color: var(--text-secondary);
    line-height: 1.4;
    margin-bottom: 1.5rem;
  }
  .hero-accent {
    color: var(--accent-cyan);
  }

  .hero-desc {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-secondary);
    max-width: 600px;
    margin-bottom: 2rem;
  }

  .hero-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 2.5rem;
  }
  .tag {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: var(--text-primary);
    padding: 0.35rem 0.85rem;
    border-radius: 8px;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    transition: background 0.2s, border-color 0.2s;
  }
  .tag:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.2);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .hero-content { 
    animation: fadeUp 0.7s 0.15s cubic-bezier(0.16, 1, 0.3, 1) both; 
  }
  
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .section-header {
    margin-bottom: 3.5rem;
    max-width: 1200px;
    margin-left: auto;
    margin-right: auto;
    width: 100%;
  }
  .section-title {
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    font-weight: 800;
    color: var(--text-primary);
  }

  .contact-subtitle {
    margin-top: 0.5rem;
    color: var(--text-secondary);
    font-size: 1.05rem;
  }

  .project-filters {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 2.5rem;
    max-width: 1200px;
    margin-left: auto;
    margin-right: auto;
    flex-wrap: wrap;
  }
  .filter-btn {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    color: var(--text-secondary);
    padding: 0.5rem 1.25rem;
    border-radius: 20px;
    font-family: var(--font-body);
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }
  .filter-btn:hover {
    background: var(--border);
    color: var(--text-primary);
  }
  .filter-btn.active {
    background: var(--accent);
    border-color: var(--accent);
    color: var(--bg-base);
  }

  .bento-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }
  @media (min-width: 900px) {
    .bento-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .project-card {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 16px;
    overflow: hidden;
    transition: opacity 0.3s, transform 0.3s;
    display: flex;
    flex-direction: column;
  }
  .project-card.filtered-out {
    display: none;
  }

  .card-large {
    grid-column: 1 / -1;
  }

  .card-body-split {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 900px) {
    .card-body-split {
      grid-template-columns: 1fr 1fr;
    }
  }

  .card-image {
    position: relative;
    width: 100%;
    overflow: hidden;
    background: var(--bg-surface);
  }
  .card-image--short {
    aspect-ratio: 16/9;
  }
  .proj-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .project-card:hover .proj-img {
    transform: scale(1.03);
  }

  .card-body {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
  }
  
  .card-content-side {
    display: flex;
    flex-direction: column;
  }

  .card-body h3 {
    font-size: 1.4rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }
  .card-body p {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .tech-pills {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: auto;
    margin-bottom: 1.5rem;
  }
  .tech-pills li {
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.1);
    color: var(--text-secondary);
    padding: 0.25rem 0.75rem;
    border-radius: 6px;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .card-links {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }
  
  .link-demo {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--text-primary);
    text-decoration: none;
    padding: 0.4rem 0.8rem;
    border-radius: 6px;
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.1);
    transition: background 0.2s;
  }
  .link-demo:hover {
    background: rgba(255,255,255,0.1);
  }

  .card-image-side {
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg-surface);
    border-radius: 12px;
    border: 1px solid var(--border);
    padding: 1.5rem;
    min-height: 240px;
  }

  .code-preview {
    width: 100%;
    background: var(--bg-elevated);
    border-radius: 8px;
    border: 1px solid var(--border);
    overflow: hidden;
  }
  
  .code-header {
    display: flex;
    gap: 6px;
    padding: 10px 12px;
    border-bottom: 1px solid var(--border);
    background: var(--bg-surface);
  }
  
  .code-body {
    padding: 1rem;
    font-family: ui-monospace, 'Cascadia Code', 'Fira Code', monospace;
    font-size: 0.85rem;
    line-height: 1.6;
    color: var(--text-primary);
  }

  .dot-red { width: 10px; height: 10px; border-radius: 50%; background: #FF5F57; }
  .dot-yellow { width: 10px; height: 10px; border-radius: 50%; background: #FFBD2E; }
  .dot-green { width: 10px; height: 10px; border-radius: 50%; background: #28C840; }
  .kw { color: #cba6f7; }
  .fn { color: #89b4fa; }
  .str { color: #a6e3a1; }

  .skills-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }
  @media (min-width: 768px) {
    .skills-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .skill-card {
    background: var(--bg-base);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 2rem;
    transition: border-color 0.3s;
  }
  .skill-card:hover {
    border-color: var(--border-hover);
  }
  .skill-icon {
    width: 48px; height: 48px;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.05);
    color: var(--text-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
  }
  .skill-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 1.25rem;
    color: var(--text-primary);
  }
  .skill-card ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  .skill-card li {
    color: var(--text-secondary);
    font-size: 0.95rem;
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
  }
  .skill-card li::before {
    content: "▹";
    color: var(--accent);
    font-weight: bold;
  }

  .contact-section {
    position: relative;
    background: #161B22; /* Lighter background for contrast */
    border-top: 1px solid var(--border);
    padding: 6rem 1.25rem;
    margin-top: 4rem;
    transition: background 0.3s ease;
  }
  .contact-section:hover {
    background: #1C2128; /* Even lighter lift on hover */
  }
  .contact-section::before {
    content: '';
    position: absolute;
    top: -1px; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.15), transparent);
    opacity: 0;
    transition: opacity 0.5s ease;
  }
  .contact-section:hover::before {
    opacity: 1;
  }
  .contact-inner {
    max-width: 1000px;
    margin: 0 auto;
  }

  .contact-wrap {
    max-width: 600px;
    margin: 0 auto;
  }
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .form-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  @media (min-width: 600px) {
    .form-row {
      grid-template-columns: 1fr 1fr;
    }
  }
  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .form-group label {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--text-primary);
  }
  .form-group input,
  .form-group textarea {
    width: 100%;
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.8rem 1rem;
    color: var(--text-primary);
    font-family: var(--font-body);
    font-size: 0.95rem;
    transition: border-color 0.2s;
  }
  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--accent);
  }
  
  .form-status {
    padding: 1rem;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 500;
  }
  .form-status.info { background: rgba(59,130,246,0.1); color: #60a5fa; border: 1px solid rgba(59,130,246,0.2); }
  .form-status.success { background: rgba(34,197,94,0.1); color: #4ade80; border: 1px solid rgba(34,197,94,0.2); }
  .form-status.error { background: rgba(239,68,68,0.1); color: #f87171; border: 1px solid rgba(239,68,68,0.2); }

  .btn-submit {
    align-self: flex-start;
  }

  .footer {
    padding: 3rem 1.25rem;
    text-align: center;
    border-top: 1px solid var(--border);
    background: var(--bg-surface);
  }
  .footer p {
    color: var(--text-secondary);
    font-size: 0.9rem;
  }
  .footer-sub {
    margin-top: 0.5rem;
    font-size: 0.8rem !important;
    color: var(--text-muted) !important;
  }
  .footer-sub a {
    color: var(--text-secondary);
    text-decoration: underline;
    text-decoration-color: var(--border);
  }
  .footer-sub a:hover {
    color: var(--text-primary);
    text-decoration-color: var(--accent);
  }
</style>
